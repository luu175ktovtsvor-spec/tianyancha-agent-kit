# Agent 使用说明

本仓库交给 Agent 后，按以下顺序执行。

## 第一步：先验证环境

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -e '.[dev,browser]'
.venv/bin/python -m playwright install chromium
bash scripts/run_demo.sh
```

成功标准：最后一行 JSON 中 `captured_records` 为 `5`、`phone_leads` 为 `2`、`rejected` 为 `3`。未通过时先修复环境，不要直接开始真实采集。

## 第二步：真实任务需要的资料

- 本次使用的数据来源；
- 浏览器入口页；如果页面可以正常打开，先由 Agent 检查页面并在任务工作区建立 CSS 选择器配置；
- 行业、主体、风险、地区和电话入库规则；
- 如需飞书，目标表和字段对应方式。

没有浏览器入口页、筛选要求或目标交付时，Agent 才需要向任务负责人补问；真实资料保存在任务工作区。

## 行业改造

公开示例只是流程示例，不是行业规则。开始真实任务前，Agent 应和任务负责人确认：

- 要查什么行业；
- 哪些词保留，哪些词排除；
- 哪些主体可以入库；
- 风险、日期、地区和电话要求；
- 页面已经打开时，请检查后自己填写选择器；无法检查时再说明需要我补充什么页面信息。
- 登录方式：请打开浏览器，我会扫码登录；我说“可以继续”后再读取结果。

确认后，Agent 可以自由修改任务工作区里的行业 profile、浏览器选择器和导出字段，不需要修改公开仓库的通用代码。不同任务可以使用完全不同的筛选逻辑；用户不需要先手写 YAML 或 CSS 选择器。

可直接把下面的内容交给 Codex、Claude Code、WorkBuddy、豆包或其他 Agent：

```text
目标行业：
要保留的词：
要排除的词：
允许的主体：
成立时间、状态和风险要求：
地区范围：
电话入库要求：
页面已经打开时，请检查后自己填写选择器；无法检查时需要我补充的信息：
飞书是否需要同步，字段怎么对应：
登录方式：请打开浏览器，我会扫码登录；我说“可以继续”后再读取结果。
```

## 本地工作流优先

README 里的成本图只用于说明会员页面和官方 API 的差异，不是执行步骤。Agent 的重点是先验证本地示例，再按任务工作区配置完成采集、筛选、导出和飞书交付。

## 第三步：执行流程

1. 根据任务负责人给出的自然语言要求，改任务工作区里的行业 profile、浏览器选择器和导出字段。
2. 使用 `tyc-agent browser-collect` 打开可见浏览器。
3. 等待使用者在浏览器里扫码登录；收到“可以继续”后，再读取结果和翻页。
4. 使用 `tyc-agent phone-leads` 生成获客电话主表、主体分表和淘汰原因。
5. 抽查输出数量和电话字段；需要时按 [飞书说明](docs/FEISHU_INTEGRATION.md) 导入。

完整参数见 [Agent 操作清单](docs/AGENT_QUICKSTART.md)。
